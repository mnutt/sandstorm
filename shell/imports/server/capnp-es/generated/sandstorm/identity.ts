// @ts-nocheck
// This file has been automatically generated by capnp-es.
import * as $ from "capnp-es";
import { Assignable, Assignable$Client, Assignable_Getter, Assignable_Getter$Client, Assignable_Setter, Assignable_Setter$Client, Blob, Blob$Client, ByteStream, ByteStream$Client, Handle, Handle$Client, KeyValue, LocalizedText, LocalizedText_Localization, StaticAsset, StaticAsset$Client, StaticAsset_Protocol } from "./util";
export const _capnpFileId = 0xc822108a5c3d7d25n;
/**
* Tag to be used in a `PowerboxDescriptor` to describe an `Identity`.
*
*/
export class Identity_PowerboxTag extends $.Struct {
  static readonly _capnp = {
    displayName: "PowerboxTag",
    id: "f35052cfb1d3bbe8",
    typeId: 0xf35052cfb1d3bbe8n,
    typeIdHex: "f35052cfb1d3bbe8",
    size: new $.ObjectSize(0, 1),
    fields: [
      { name: "permissions", codeOrder: 0, ordinal: 0, kind: "slot", offset: 0, type: { kind: "list", elementType: { kind: "bool" } } }
    ] as const,
  };
  _adoptPermissions(value: $.Orphan<$.List<boolean>>): void {
    $.utils.adopt(value, $.utils.getPointer(0, this));
  }
  _disownPermissions(): $.Orphan<$.List<boolean>> {
    return $.utils.disown(this.permissions);
  }
  /**
* In a query, the permissions that the requester wishes to be held by the identity. When
* the powerbox UI asks the user to select a role, it hides any roles that do not provide all of
* these permissions.
*
* In a fulfillment, the current permissions actually held by the identity.
*
*/
  get permissions(): $.List<boolean> {
    return $.utils.getList(0, $.BoolList, this);
  }
  _hasPermissions(): boolean {
    return !$.utils.isNull($.utils.getPointer(0, this));
  }
  _initPermissions(length: number): $.List<boolean> {
    return $.utils.initList(0, $.BoolList, length, this);
  }
  set permissions(value: $.List<boolean>) {
    $.utils.copyFrom(value, $.utils.getPointer(0, this));
  }
  toString(): string { return "Identity_PowerboxTag_" + super.toString(); }
}
export class Identity_GetProfile$Params extends $.Struct {
  static readonly _capnp = {
    displayName: "getProfile$Params",
    id: "f32d79a7b575d94c",
    typeId: 0xf32d79a7b575d94cn,
    typeIdHex: "f32d79a7b575d94c",
    size: new $.ObjectSize(0, 0),
    fields: [] as const,
  };
  toString(): string { return "Identity_GetProfile$Params_" + super.toString(); }
}
export class Identity_GetProfile$Results extends $.Struct {
  static readonly _capnp = {
    displayName: "getProfile$Results",
    id: "cd7272b855c92f6d",
    typeId: 0xcd7272b855c92f6dn,
    typeIdHex: "cd7272b855c92f6d",
    size: new $.ObjectSize(0, 1),
    fields: [
      { name: "profile", codeOrder: 0, ordinal: 0, kind: "slot", offset: 0, type: { kind: "struct", typeId: 0xd3d0c34d7201fcefn, typeIdHex: "d3d0c34d7201fcef", displayName: "Profile" } }
    ] as const,
  };
  _adoptProfile(value: $.Orphan<Profile>): void {
    $.utils.adopt(value, $.utils.getPointer(0, this));
  }
  _disownProfile(): $.Orphan<Profile> {
    return $.utils.disown(this.profile);
  }
  get profile(): Profile {
    return $.utils.getStruct(0, Profile, this);
  }
  _hasProfile(): boolean {
    return !$.utils.isNull($.utils.getPointer(0, this));
  }
  _initProfile(): Profile {
    return $.utils.initStructAt(0, Profile, this);
  }
  set profile(value: Profile) {
    $.utils.copyFrom(value, $.utils.getPointer(0, this));
  }
  toString(): string { return "Identity_GetProfile$Results_" + super.toString(); }
}
export class Identity_GetProfile$Results$Promise {
  pipeline: $.Pipeline<any, any, Identity_GetProfile$Results>;
  constructor(pipeline: $.Pipeline<any, any, Identity_GetProfile$Results>) {
    this.pipeline = pipeline;
  }
  async promise(): Promise<Identity_GetProfile$Results> {
    return await this.pipeline.struct();
  }
}
export class Identity$Client {
  client: $.Client;
  static readonly interfaceId: bigint = 0xc084987aa951dd18n;
  constructor(client: $.Client) {
    this.client = client;
  }
  static readonly methods: [
    $.Method<Identity_GetProfile$Params, Identity_GetProfile$Results>
  ] = [
    {
      ParamsClass: Identity_GetProfile$Params,
      ResultsClass: Identity_GetProfile$Results,
      interfaceId: Identity$Client.interfaceId,
      methodId: 0,
      interfaceName: "sandstorm/identity.capnp:Identity",
      methodName: "getProfile",
      paramFields: Identity_GetProfile$Params._capnp.fields,
      resultFields: Identity_GetProfile$Results._capnp.fields
    }
  ];
  /**
* Get the identity's current profile.
*
*/
  getProfile(paramsFunc?: (params: Identity_GetProfile$Params) => void): Identity_GetProfile$Results$Promise {
    const answer = this.client.call({
      method: Identity$Client.methods[0],
      paramsFunc: paramsFunc
    });
    const pipeline = new $.Pipeline(Identity_GetProfile$Results, answer);
    return new Identity_GetProfile$Results$Promise(pipeline);
  }
}
$.Registry.register(Identity$Client.interfaceId, Identity$Client);
export interface Identity$Server$Target {
  getProfile(params: Identity_GetProfile$Params, results: Identity_GetProfile$Results): Promise<void>;
}
export class Identity$Server extends $.Server {
  readonly target: Identity$Server$Target;
  constructor(target: Identity$Server$Target) {
    super(target, [
      {
        ...Identity$Client.methods[0],
        impl: target.getProfile
      }
    ]);
    this.target = target;
  }
  client(): Identity$Client {
    return new Identity$Client(this);
  }
}
/**
* Represents a user identity.
*
* Things you can do:
* - Mention the identity on activity events to send notifications. See ActivityEvent in
*   activity.capnp.
* - Powerbox-request an Identity to have the user choose from among their contacts. Note that the
*   user will be prompted to share the grain with the target identity.
* - offer() the identity to the user in order to let them see the identity's profile card and
*   choose to add the identity to their contacts. You could do this e.g. when the user clicks
*   on the identity's name in your app's UI. (TODO(someday): Not implemented yet.)
*
* This capability is always persistable.
*
*/
export class Identity extends $.Interface {
  static readonly PowerboxTag = Identity_PowerboxTag;
  static readonly Client = Identity$Client;
  static readonly Server = Identity$Server;
  static readonly _capnp = {
    displayName: "Identity",
    id: "c084987aa951dd18",
    typeId: 0xc084987aa951dd18n,
    typeIdHex: "c084987aa951dd18",
    size: new $.ObjectSize(0, 0),
    methods: Identity$Client.methods,
  };
  toString(): string { return "Identity_" + super.toString(); }
}
export const Profile_Pronouns = {
  /**
* "they"
*
*/
  NEUTRAL: 0,
  /**
* "he" / "him"
*
*/
  MALE: 1,
  /**
* "she" / "her"
*
*/
  FEMALE: 2,
  /**
* "it"
*
*/
  ROBOT: 3
} as const;
export type Profile_Pronouns = (typeof Profile_Pronouns)[keyof typeof Profile_Pronouns];
/**
* Personal information provided by a user, intended to be displayed to other users when
* they come in contact.
*
*/
export class Profile extends $.Struct {
  static readonly Pronouns = Profile_Pronouns;
  static readonly _capnp = {
    displayName: "Profile",
    id: "d3d0c34d7201fcef",
    typeId: 0xd3d0c34d7201fcefn,
    typeIdHex: "d3d0c34d7201fcef",
    size: new $.ObjectSize(8, 3),
    fields: [
      { name: "displayName", codeOrder: 0, ordinal: 0, kind: "slot", offset: 0, type: { kind: "struct", typeId: 0x8b5db772377be249n, typeIdHex: "8b5db772377be249", displayName: "LocalizedText" } },
      { name: "preferredHandle", codeOrder: 1, ordinal: 1, kind: "slot", offset: 1, type: { kind: "text" } },
      { name: "picture", codeOrder: 2, ordinal: 2, kind: "slot", offset: 2, type: { kind: "interface", typeId: 0xfabb5e621fa9a23fn, typeIdHex: "fabb5e621fa9a23f", displayName: "StaticAsset" } },
      { name: "pronouns", codeOrder: 3, ordinal: 3, kind: "slot", offset: 0, type: { kind: "enum", typeId: 0x84752dcf8539ab01n, typeIdHex: "84752dcf8539ab01", displayName: "Pronouns" } }
    ] as const,
  };
  _adoptDisplayName(value: $.Orphan<LocalizedText>): void {
    $.utils.adopt(value, $.utils.getPointer(0, this));
  }
  _disownDisplayName(): $.Orphan<LocalizedText> {
    return $.utils.disown(this.displayName);
  }
  /**
* Name by which to identify this user within the user interface.  For example, if two users are
* editing a document simultaneously, the application may display each user's cursor position to
* the other, labeled with the respective display names.  As the users edit the document, the
* document's history may be annotated with the display name of the user who made each change.
* Display names are NOT unique nor stable:  two users could potentially have the same display
* name and a user's display name could change.
*
*/
  get displayName(): LocalizedText {
    return $.utils.getStruct(0, LocalizedText, this);
  }
  _hasDisplayName(): boolean {
    return !$.utils.isNull($.utils.getPointer(0, this));
  }
  _initDisplayName(): LocalizedText {
    return $.utils.initStructAt(0, LocalizedText, this);
  }
  set displayName(value: LocalizedText) {
    $.utils.copyFrom(value, $.utils.getPointer(0, this));
  }
  /**
* The user's preferred "handle", as set in their account settings. This is guaranteed to be
* composed only of lowercase English letters, digits, and underscores, and will not start with
* a digit. It is NOT guaranteed to be unique; if your app dislikes duplicate handles, it must
* check for them and do something about them.
*
*/
  get preferredHandle(): string {
    return $.utils.getText(1, this);
  }
  set preferredHandle(value: string) {
    $.utils.setText(1, value, this);
  }
  /**
* The user's profile picture, appropriate for displaying in a 64x64 context.
*
*/
  get picture(): StaticAsset$Client {
    return new StaticAsset$Client($.utils.getInterfaceClientOrNullAt(2, this));
  }
  set picture(value: StaticAsset$Client) {
    $.utils.setInterfacePointer(this.segment.message.addCap(value.client), $.utils.getPointer(2, this));
  }
  /**
* Indicates which pronouns the user prefers you use to refer to them.
*
*/
  get pronouns(): Profile_Pronouns {
    return $.utils.getUint16(0, this) as Profile_Pronouns;
  }
  set pronouns(value: Profile_Pronouns) {
    $.utils.setUint16(0, value, this);
  }
  toString(): string { return "Profile_" + super.toString(); }
}
/**
* Information about the user opening a new session, including a snapshot of the user's
* profile.
*
* TODO(soon):  More details:
* - Sharing/authority chain:  "Carol (via Bob, via Alice)"
*
*/
export class UserInfo extends $.Struct {
  static readonly _capnp = {
    displayName: "UserInfo",
    id: "94b9d1efb35d11d3",
    typeId: 0x94b9d1efb35d11d3n,
    typeIdHex: "94b9d1efb35d11d3",
    size: new $.ObjectSize(8, 7),
    fields: [
      { name: "displayName", codeOrder: 0, ordinal: 0, kind: "slot", offset: 0, type: { kind: "struct", typeId: 0x8b5db772377be249n, typeIdHex: "8b5db772377be249", displayName: "LocalizedText" } },
      { name: "preferredHandle", codeOrder: 1, ordinal: 4, kind: "slot", offset: 4, type: { kind: "text" } },
      { name: "pictureUrl", codeOrder: 2, ordinal: 6, kind: "slot", offset: 5, type: { kind: "text" } },
      { name: "pronouns", codeOrder: 3, ordinal: 5, kind: "slot", offset: 0, type: { kind: "enum", typeId: 0x84752dcf8539ab01n, typeIdHex: "84752dcf8539ab01", displayName: "Pronouns" } },
      { name: "deprecatedPermissionsBlob", codeOrder: 4, ordinal: 1, kind: "slot", offset: 1, type: { kind: "data" } },
      { name: "permissions", codeOrder: 5, ordinal: 3, kind: "slot", offset: 3, type: { kind: "list", elementType: { kind: "bool" } } },
      { name: "identityId", codeOrder: 6, ordinal: 2, kind: "slot", offset: 2, type: { kind: "data" } },
      { name: "identity", codeOrder: 7, ordinal: 7, kind: "slot", offset: 6, type: { kind: "interface", typeId: 0xc084987aa951dd18n, typeIdHex: "c084987aa951dd18", displayName: "Identity" } }
    ] as const,
  };
  _adoptDisplayName(value: $.Orphan<LocalizedText>): void {
    $.utils.adopt(value, $.utils.getPointer(0, this));
  }
  _disownDisplayName(): $.Orphan<LocalizedText> {
    return $.utils.disown(this.displayName);
  }
  /**
* The current value of `identity.getProfile().displayName`, provided here for convenience.
*
*/
  get displayName(): LocalizedText {
    return $.utils.getStruct(0, LocalizedText, this);
  }
  _hasDisplayName(): boolean {
    return !$.utils.isNull($.utils.getPointer(0, this));
  }
  _initDisplayName(): LocalizedText {
    return $.utils.initStructAt(0, LocalizedText, this);
  }
  set displayName(value: LocalizedText) {
    $.utils.copyFrom(value, $.utils.getPointer(0, this));
  }
  /**
* The current value of `identity.getProfile().preferredHandle`, provided here for convenience.
*
*/
  get preferredHandle(): string {
    return $.utils.getText(4, this);
  }
  set preferredHandle(value: string) {
    $.utils.setText(4, value, this);
  }
  /**
* The current value of `identity.getProfile().staticAsset.getUrl()`, provided here for
* convenience.
*
* TODO(security) TODO(apibump): If we allow UserInfo to come from untrusted sources then this
*   field is XSS-prone. Currently UserInfo only comes from the front-end.
*
*/
  get pictureUrl(): string {
    return $.utils.getText(5, this);
  }
  set pictureUrl(value: string) {
    $.utils.setText(5, value, this);
  }
  /**
* The current value of `identity.getProfile().pronouns`, provided here for convenience.
*
*/
  get pronouns(): Profile_Pronouns {
    return $.utils.getUint16(0, this) as Profile_Pronouns;
  }
  set pronouns(value: Profile_Pronouns) {
    $.utils.setUint16(0, value, this);
  }
  _adoptDeprecatedPermissionsBlob(value: $.Orphan<$.Data>): void {
    $.utils.adopt(value, $.utils.getPointer(1, this));
  }
  _disownDeprecatedPermissionsBlob(): $.Orphan<$.Data> {
    return $.utils.disown(this.deprecatedPermissionsBlob);
  }
  get deprecatedPermissionsBlob(): $.Data {
    return $.utils.getData(1, this);
  }
  _hasDeprecatedPermissionsBlob(): boolean {
    return !$.utils.isNull($.utils.getPointer(1, this));
  }
  _initDeprecatedPermissionsBlob(length: number): $.Data {
    return $.utils.initData(1, length, this);
  }
  set deprecatedPermissionsBlob(value: $.Data) {
    $.utils.copyFrom(value, $.utils.getPointer(1, this));
  }
  _adoptPermissions(value: $.Orphan<$.List<boolean>>): void {
    $.utils.adopt(value, $.utils.getPointer(3, this));
  }
  _disownPermissions(): $.Orphan<$.List<boolean>> {
    return $.utils.disown(this.permissions);
  }
  /**
* Set of permissions which this user has.  The exact set might not correspond directly to any
* particular role for a number of reasons:
* - The sharer may have toggled individual permissions through the advanced settings.
* - If two different users share different roles to a third user, and neither of the roles is a
*   strict superset of the other, the user gets the union of the two permissions.
* - If Alice shares role A to Bob, and Bob further delegates role B to Carol, then Carol's
*   permissions are the intersection of those granted by roles A and B.
*
* That said, some combinations of permissions may not make sense.  For example, a document editor
* probably has no reasonable way to implement write permission without read permission.  It is up
* to the application to decide what to do in this case, but simply ignoring the nonsensical
* permissions is often a fine strategy.
*
* If the user's permissions are reduced while the session is opened, the session will be closed
* by the platform and the user forced to start a new one.  If the user's permissions are increased
* while the session is opened, the system will prompt them to start a new session to use the new
* permissions.  Either way, the application need not worry about permissions changing during a
* session.
*
*/
  get permissions(): $.List<boolean> {
    return $.utils.getList(3, $.BoolList, this);
  }
  _hasPermissions(): boolean {
    return !$.utils.isNull($.utils.getPointer(3, this));
  }
  _initPermissions(length: number): $.List<boolean> {
    return $.utils.initList(3, $.BoolList, length, this);
  }
  set permissions(value: $.List<boolean>) {
    $.utils.copyFrom(value, $.utils.getPointer(3, this));
  }
  _adoptIdentityId(value: $.Orphan<$.Data>): void {
    $.utils.adopt(value, $.utils.getPointer(2, this));
  }
  _disownIdentityId(): $.Orphan<$.Data> {
    return $.utils.disown(this.identityId);
  }
  /**
* A unique, stable identifier for the calling user. This is computed such that a user's ID will
* be the same across all Sandstorm servers, and will not collide with any other identity ID in the
* world. Therefore, grains transferred between servers can still count on the user IDs being the
* same and secure (unless the new host is itself malicious, of course, in which case all bets are
* off).
*
* The ID is actually a SHA-256 hash, therefore it is always exactly 32 bytes and the app can
* safely truncate it down to some shorter prefix according to its own security/storage trade-off
* needs.
*
* If the user is not logged in, `identityId` is null.
*
*/
  get identityId(): $.Data {
    return $.utils.getData(2, this);
  }
  _hasIdentityId(): boolean {
    return !$.utils.isNull($.utils.getPointer(2, this));
  }
  _initIdentityId(length: number): $.Data {
    return $.utils.initData(2, length, this);
  }
  set identityId(value: $.Data) {
    $.utils.copyFrom(value, $.utils.getPointer(2, this));
  }
  /**
* The identity capability for this user. null if the user is not logged in.
*
*/
  get identity(): Identity$Client {
    return new Identity$Client($.utils.getInterfaceClientOrNullAt(6, this));
  }
  set identity(value: Identity$Client) {
    $.utils.setInterfacePointer(this.segment.message.addCap(value.client), $.utils.getPointer(6, this));
  }
  toString(): string { return "UserInfo_" + super.toString(); }
}
